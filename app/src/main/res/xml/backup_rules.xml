<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Include only non-sensitive app data -->
    <include domain="database" path="sakartvelo_guide.db" />

    <!-- ARCHITECT'S FIX: Exclude DataStore and sensitive preferences -->
    <exclude domain="file" path="datastore/" />
    <exclude domain="sharedpref" path="device.xml" />
</full-backup-content>