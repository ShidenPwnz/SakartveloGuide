<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ARCHITECT'S FIX: Visibility Queries for External Apps -->
    <queries>
        <!-- Navigation -->
        <package android:name="com.google.android.apps.maps" />
        <package android:name="com.waze" />

        <!-- Taxi & Transport -->
        <package android:name="ee.mtakso.client" /> <!-- Bolt -->
        <package android:name="com.yandex.taxi" /> <!-- Yandex Go -->

        <!-- Accommodation -->
        <package android:name="com.booking" />
        <package android:name="com.airbnb.android" />

        <!-- Flights -->
        <package android:name="net.skyscanner.android.main" />
        <package android:name="com.wizzair.WizzAirApp" />

        <!-- Browser Fallback -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".SakartveloApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SakartveloGuide"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SakartveloGuide.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>